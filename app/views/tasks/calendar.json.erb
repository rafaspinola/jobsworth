<%= @tasks.collect{|task|
  {:id => task.task_num,
   :title => task.name,
   :start => task.start_date.strftime('%Y-%m-%dT00:00:00'),
   :end => task.due_date.strftime('%Y-%m-%dT00:00:00'),
   :resource => task.task_num
  }
}.to_json.html_safe
-%>
